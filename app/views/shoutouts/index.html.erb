<p id="notice"><%= notice %></p>

<h1>Listing Shoutouts</h1>

<table border=1>
  <thead>
    <tr>
      <th>Content</th>
      <th>Created at</th>
      <th>Created by</th>
    </tr>
  </thead>

  <tbody>
    <% @shoutouts.each do |shoutout| %>
      <tr>
        <td><%= shoutout.content %></td>
        <td><%= shoutout.created_at %></td>
        <td><% @user.each do |x| %>
            <% if x.shoutout_ids.include?(shoutout.id) %>
              <%= x.user_name %>
            <% else %>
              <% "Anonymouse" %>
            <% end %>
          <% end %>
        <% end %>  
        </td>
      </tr>
  </tbody>
</table>

<br>

<%= link_to 'back', :back %>
